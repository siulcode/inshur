create table catwarrior_facts(
  cuid VARCHAR(100) NOT NULL,
  date_of_birth VARCHAR(100),
  fact VARCHAR(255) NOT NULL,
  first_name VARCHAR(100) NOT NULL,
  last_name VARCHAR(100) NOT NULL,
  id INT NOT NULL AUTO_INCREMENT,
  PRIMARY KEY ( id )
);
